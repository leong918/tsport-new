APP_NAME=Laravel
APP_ENV=local
APP_KEY=base64:ZCANfXcurpec4s8YUjC4/BLODkuJLlxdWCbpSnqE6Yc=
APP_DEBUG=true
APP_URL=http://tsport-new.localhost

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=tsport-new
DB_USERNAME=root
DB_PASSWORD=

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

MEMCACHED_HOST=127.0.0.1

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=sandbox.smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USERNAME=192a5ab3adce78
MAIL_PASSWORD=29dc0bf4c4ff61
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# OBS Streaming Configuration
# Leave empty to auto-generate from APP_URL, or set custom RTMP server
OBS_SERVER_URL=rtmp://localhost:1935/live

# DVR Webhook Configuration
# Secret key for validating webhook requests from rtmp-server
DVR_WEBHOOK_SECRET=your-secret-key-here

FILESYSTEM_DRIVER=public

# Streaming Configuration
STREAMING_FLV_ENDPOINT=http://localhost:8080
STREAMING_HLS_ENDPOINT=http://localhost:8080
STREAMING_RTMP_SERVER=rtmp://localhost:1935
